#include <iostream>
#include <vector>
#include <string>
#include <queue>


using namespace std;

int main () {
  // int n;
  // cin >> n;
  // string s;
  // cin >> s;

  int n = 2;
  string s = "1:0,0:1";

  vector<vector<int> > path(n);
  vector<int> inDegree(n, 0);

  

  return 0;
}